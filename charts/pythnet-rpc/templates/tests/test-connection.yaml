apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pythnet-rpc.fullname" . }}-test-connection"
  labels:
    {{- include "pythnet-rpc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pythnet-rpc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
